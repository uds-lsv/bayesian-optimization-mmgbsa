See the `process_data.ipynb` notebook for more details on the preprocessing before running the Bayesian Optimization.

The raw ligand files (input structures used for MM/GBSA scoring) are archived in
`simulation_configuration/ligand_files_split*.tar.gz`.